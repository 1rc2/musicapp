<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.musicflow.app"
    android:versionCode="10204"
    android:versionName="1.2.4">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <uses-sdk android:minSdkVersion="21" android:targetSdkVersion="34"/>

    <application
        android:allowBackup="true"
        android:label="MusicFlow"
        android:icon="@mipmap/ic_launcher_anim"
        android:roundIcon="@mipmap/ic_launcher_anim"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true">

        <!-- 主 Activity（无 LAUNCHER intent-filter，由 alias 代理） -->
        <activity
            android:name="com.musicflow.app.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- 动态图标入口（默认启用） -->
        <activity-alias
            android:name="com.musicflow.app.MainActivityAnim"
            android:targetActivity="com.musicflow.app.MainActivity"
            android:icon="@mipmap/ic_launcher_anim"
            android:roundIcon="@mipmap/ic_launcher_anim"
            android:label="MusicFlow"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <!-- 静态图标入口（默认禁用） -->
        <activity-alias
            android:name="com.musicflow.app.MainActivityStatic"
            android:targetActivity="com.musicflow.app.MainActivity"
            android:icon="@mipmap/ic_launcher_static"
            android:roundIcon="@mipmap/ic_launcher_static"
            android:label="MusicFlow"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <provider
            android:name="com.musicflow.app.GenericFileProvider"
            android:authorities="com.musicflow.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>
    </application>
</manifest>